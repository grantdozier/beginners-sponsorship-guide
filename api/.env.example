# Copy to .env.local and fill in — .env.local is gitignored.
# Railway provides DATABASE_URL automatically when the Postgres plugin is attached.

DATABASE_URL=postgresql://user:password@host:5432/sponsorguide
PORT=8080
NODE_ENV=development
